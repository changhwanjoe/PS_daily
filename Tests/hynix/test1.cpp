#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
using namespace std;
int main(void) {
	queue <int> q;
	vector <int> v;
	//cout << "v[i], v.size(), v.capacity()" << endl;
	//q.push(1);
	//q.push(13);
	//cout << q.front() << endl;
	
	/*for (int i = 0; i <= 10 i++) {
		v.push_back(i);
		cout << v[i] << ", " << v.size() << " ," << v.capacity() << endl;
	}
	*/
	v = { 1,2,3,3 };
	int index = 1;

	while (1)
	{
		cout << "heelo" << endl;
		for (int i = 0; i < 10; i++) {
			cout << "march" << endl;
			if
			continue;
			cout << "june" << "endl";
		}
		continue;
		cout << "hi" << endl;

	}
}